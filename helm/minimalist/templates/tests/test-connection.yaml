apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "minimalist.fullname" . }}-test-connection"
  labels:
    {{- include "minimalist.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "minimalist.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
