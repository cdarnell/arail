"""arail.research — goal-driven research recipe machinery.

The "recipe" is the triple ``program.md`` (what) + ``prepare.py`` (how
to measure) + ``train.py`` (how to apply variants). This module owns
drafting and parsing — the autoresearch loop in
``arail.experiments.autoresearch`` is the consumer.
"""
