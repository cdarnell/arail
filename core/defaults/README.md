# Defaults Directory

This directory contains resource profiles and global mesh/DNS settings for the Nucleus Core baseline.

- `resource-profiles.yaml`: RAM-based sizing and feature toggles
- `global-values.yaml`: Shared mesh and observability settings
